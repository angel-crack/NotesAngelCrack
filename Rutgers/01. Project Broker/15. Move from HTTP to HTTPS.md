Install OPENSSL

```bash
sudo apt update
sudo apt install openssl -y
```

```bash
mkdir -p /home/ruadmin/app/backend/certs
openssl req -nodes -new -x509 \
  -keyout /home/ruadmin/app/backend/certs/server.key \
  -out /home/ruadmin/app/backend/certs/server.cert \
  -days 365
```