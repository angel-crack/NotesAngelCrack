Allow ICMP

```bash
netsh advfirewall firewall add rule name="Allow ICMPv4-In" protocol=icmpv4:8,any dir=in action=allow
```

Allow RDP

```bash
netsh advfirewall firewall set rule group="Remote Desktop" new enable=yes
```

Allow SFTP

```bash
netsh advfirewall firewall add rule name="Allow SFTP (SSH)" dir=in action=allow protocol=TCP localport=22
```


```bash
ip nat inside source static tcp 10.228.0.101 3389 10.224.71.86 3389 extendable
```