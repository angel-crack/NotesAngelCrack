```sql
run sql select eu.userid as user, d.name as device, dnpmap.numplanindex as index, np.dnorpattern as line, css.name as css, eu.telephonenumber, eu.keypadenteredalternateidentifier as selfservice, tpusage.name as linetype from devicenumplanmapendusermap as dnpumap left join devicenumplanmap as dnpmap on dnpmap.pkid = dnpumap.fkdevicenumplanmap left join device as d on d.pkid = dnpmap.fkdevice left join numplan as np on np.pkid = dnpmap.fknumplan left join callingsearchspace as css on css.pkid = np.fkcallingsearchspace_sharedlineappear left join enduser as eu on eu.pkid = dnpumap.fkenduser left join typepatternusage as tpusage on tpusage.enum = np.tkpatternusage order by eu.userid ASC
```

