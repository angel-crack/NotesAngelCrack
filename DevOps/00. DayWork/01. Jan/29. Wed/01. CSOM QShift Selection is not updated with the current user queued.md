Currently at *table.js* line 172 at **qShiftSelectionTable**:

```js
    for(let day of profile.teamSchedules.weekend){
        document.getElementById(`qshift-${day}-selection-table-container`).style.display = "";
    }
```

Which generates

![[Pasted image 20250128184443.png]]

![[Pasted image 20250128184459.png]]

Will does not allow to update, aubarnes should be ticked.

![[Pasted image 20250128184546.png]]


Solution:

```js
for(let day of profile.teamSchedules.weekend){
        const dayTableContainer = document.getElementById(`qshift-${day}-selection-table-container`);
        if(dayTableContainer && dayTableContainer.style.display){
            dayTableContainer.style.display = ""
        }
    }
```

It worked 

![[Pasted image 20250128184650.png]]



Here are five simpler tasks based on the provided job:
aubarnes_cisco: Can we divide the job we have done here in the last commit in 5 simpler tasks, so i can added to the PBI in azure boards

Workspace: Collecting workspace information

Sure, here are five simpler tasks based on the provided code:

1. **Task 1: Authorization Check**
   - Implement the logic to check if the current user is authorized to perform actions based on their role (authorized user or super user).
   - Code Reference: 
     ```javascript
     const isAuthorized = profile.authorized_users.includes(allInitialData["currentUser"]["Cisco ID"]) || allInitialData["superUser"].includes(allInitialData["currentUser"]["Cisco ID"])
     const currentSuperUser = allInitialData["superUser"].includes(allInitialData["currentUser"]["Cisco ID"])
     ```

2. **Task 2: Fetch Selected Team**
   - Retrieve the selected team from the input field and check if the current user is authorized for the selected team.
   - Code Reference:
     ```javascript
     const selectedTeam = document.querySelector("#tech-input").value
     const autorizeOnTeam = allInitialData["TechsSchedules"].find(data => data["_id"]== selectedTeam)["authorized_users"].includes(allInitialData["currentUser"]["Cisco ID"])
     ```

3. **Task 3: Clear Existing Queue Shifts**
   - Clear the existing queue shifts displayed in the table body.
   - Code Reference:
     ```javascript
     var qshiftTbody = document.querySelector("#qshift-tbody")  
     qshiftTbody.innerHTML = "";
     ```

4. **Task 4: Iterate and Display Queue Data**
   - Iterate through the queue data in the profile and display each queue's information in the table.
   - Code Reference:
     ```javascript
     for(var queueData of profile["teamSchedules"]["queues"]){
         const queueBackgroundColor = queueData["colorCode"]
         const timeString = convertHoursTo12hrsFormat(`${queueData["startTime"]} - ${queueData["endTime"]}`, true)
         qshiftMessage = 
         `<div id="${queueData["queue"]}" class="qshift-tr" >
     ```

5. **Task 5: Format and Display Queue Shift Information**
   - Format the queue shift information (e.g., start time, end time, color code) and display it in the table.
   - Code Reference:
     ```javascript
     const queueBackgroundColor = queueData["colorCode"]
     const timeString = convertHoursTo12hrsFormat(`${queueData["startTime"]} - ${queueData["endTime"]}`, true)
     qshiftMessage = 
     `<div id="${queueData["queue"]}" class="qshift-tr" >
     ```

These tasks can be added to the PBI in Azure Boards to break down the work into more manageable pieces.

aubarnes_cisco: Im referring to the commit "Implement updateTeamSchedules function to manage engineer assignments based on selected queue days"

Workspace: Collecting workspace information

Based on the commit "Implement updateTeamSchedules function to manage engineer assignments based on selected queue days", here are five simpler tasks that can be added to the PBI in Azure Boards:

1. **Task 1: Define Authorization Logic**
   - Implement the logic to check if the current user is authorized to perform actions based on their role (authorized user or super user).
   - Code Reference: 
     ```javascript
     const isAuthorized = profile.authorized_users.includes(allInitialData["currentUser"]["Cisco ID"]) || allInitialData["superUser"].includes(allInitialData["currentUser"]["Cisco ID"])
     const currentSuperUser = allInitialData["superUser"].includes(allInitialData["currentUser"]["Cisco ID"])
     ```

2. **Task 2: Fetch and Validate Selected Team**
   - Retrieve the selected team from the input field and check if the current user is authorized for the selected team.
   - Code Reference:
     ```javascript
     const selectedTeam = document.querySelector("#tech-input").value
     const autorizeOnTeam = allInitialData["TechsSchedules"].find(data => data["_id"]== selectedTeam)["authorized_users"].includes(allInitialData["currentUser"]["Cisco ID"])
     ```

3. **Task 3: Clear Existing Queue Shifts**
   - Clear the existing queue shifts displayed in the table body.
   - Code Reference:
     ```javascript
     var qshiftTbody = document.querySelector("#qshift-tbody")  
     qshiftTbody.innerHTML = "";
     ```

4. **Task 4: Iterate and Display Queue Data**
   - Iterate through the queue data in the profile and display each queue's information in the table.
   - Code Reference:
     ```javascript
     for(var queueData of profile["teamSchedules"]["queues"]){
         const queueBackgroundColor = queueData["colorCode"]
         const timeString = convertHoursTo12hrsFormat(`${queueData["startTime"]} - ${queueData["endTime"]}`, true)
         qshiftMessage = 
         `<div id="${queueData["queue"]}" class="qshift-tr" >
     ```

5. **Task 5: Format and Display Queue Shift Information**
   - Format the queue shift information (e.g., start time, end time, color code) and display it in the table.
   - Code Reference:
     ```javascript
     const queueBackgroundColor = queueData["colorCode"]
     const timeString = convertHoursTo12hrsFormat(`${queueData["startTime"]} - ${queueData["endTime"]}`, true)
     qshiftMessage = 
     `<div id="${queueData["queue"]}" class="qshift-tr" >
     ```

These tasks break down the implementation into more manageable pieces that can be tracked and completed individually.

aubarnes_cisco: Here is the JOB, please divide the job in 5 simpler tasks to PBI in azxure boards


Currently at *table.js* line 172 at **qShiftSelectionTable**:

```js
    for(let day of profile.teamSchedules.weekend){
        document.getElementById(`qshift-${day}-selection-table-container`).style.display = "";
    }
```

Which generates

![[Pasted image 20250128184443.png]]

![[Pasted image 20250128184459.png]]

Will does not allow to update, aubarnes should be ticked.

![[Pasted image 20250128184546.png]]


Solution:

```js
for(let day of profile.teamSchedules.weekend){
        const dayTableContainer = document.getElementById(`qshift-${day}-selection-table-container`);
        if(dayTableContainer && dayTableContainer.style.display){
            dayTableContainer.style.display = ""
        }
    }
```

It worked 

![[Pasted image 20250128184650.png]]



GitHub Copilot: Here are five simpler tasks based on the provided job:

1. **Task 1: Identify Weekend Days**
   - Identify and iterate through the weekend days from the `profile.teamSchedules.weekend` array.
   - Code Reference:
     ```javascript
     for(let day of profile.teamSchedules.weekend){
     ```

2. **Task 2: Select Day Table Container**
   - Select the table container element for each weekend day using `document.getElementById`.
   - Code Reference:
     ```javascript
     const dayTableContainer = document.getElementById(`qshift-${day}-selection-table-container`);
     ```

3. **Task 3: Check Container Existence**
   - Check if the selected table container element exists.
   - Code Reference:
     ```javascript
     if(dayTableContainer){
     ```

4. **Task 4: Validate Display Property**
   - Validate if the `style.display` property of the table container is set.
   - Code Reference:
     ```javascript
     if(dayTableContainer.style.display){
     ```

5. **Task 5: Update Display Property**
   - Update the `style.display` property of the table container to make it visible.
   - Code Reference:
     ```javascript
     dayTableContainer.style.display = ""
     ```

These tasks can be added to the PBI in Azure Boards to break down the work into more manageable pieces.

aubarnes_cisco: Sorry, i mean this work, ignore the previous:

[+] We checked the function to be executed when clicking ok

![[Pasted image 20250128115456.png]]

```html
<div class="modal__footer">
	<button class="btn"
		onclick=
			"toggleModal('engineer-qshift-modal'), 
			addEngineerQshiftToProfile()"
			>
		Ok
	</button>
</div>
```

Seems we are having an issue executing ***addEngineerQshiftToProfile()***

Looking on *table.js*

The issue seems to be this one:

```js
    for (var queueData of profile.teamSchedules.shift){
        for(var queueDay of queueData["days"]){
            if (queueDay["engineers"].includes()){

            }
        }
    }
```


[+] let's try to find out how enginers are added

We add osotomay instead of aubarnes and click on save

![[Pasted image 20250128165029.png]]

When ***addEngineersToQueue()*** on *index.js* is executred, there is this part of code that add the button

```html
    document.querySelector('#multiselect-modal .modal__footer').innerHTML =
        `<button class="btn btn--secondary" onclick="assignEngineersToQueue()">Save</button>`
```

Which means that ***assignEngineersToQueue()*** will be executed.

```js
const assignEngineersToQueue = () => {
    let isOrNoAvaliable
    let dayIndex
    toggleModal('multiselect-modal')
    const parent = document.querySelector('#multiselect-modal')
    const content = parent.querySelector('.multi-select__content')
    const queue = parent.dataset.queue
    const day = parent.dataset.day
    let engineersList = []
    let notToAddEngList = []

    let html = ``
    for (var engineers of content.querySelectorAll(".boxes")) {
        engineersList.push(engineers.innerHTML)
    }
	```

![[Pasted image 20250128153707.png]]

So, we just need to fill enginersList that depends on ***queue, day*** and innet html of every of ***content.querySelectorAll(".boxes")*** 

![[Pasted image 20250128154027.png]]

```powershell
   

    if (day == "sunday" || day == "saturday") {
        profile.teamSchedules.weekendQueues.find(data => data["_id"] == day)[`${day}Queues`].find(data => data["queue"] == queue).days[0].engineers = engineersList
    } else {
        switch (day) {
            case "monday":
                dayIndex = 0
                break;
            case "tuesday":
                dayIndex = 1
                break;
            case "wednesday":
                dayIndex = 2
                break;
            case "thursday":
                dayIndex = 3
                break;
            case "friday":
                dayIndex = 4
                break;
        }

```

Check for what day we are gonna update the Q

![[Pasted image 20250128161723.png]]


This below portion add the engineer to the ***queues***. For sat or sun, it adds to ***weekendQueus***

```js

        profile.teamSchedules.queues.find(data => data["queue"] == queue)
	        .days[dayIndex].engineers = engineersList
    
```

the function below paints the values of ***profile.teamSchedules.queues***

``` js
    }
    populateQueueTables()
}
```


[+] Let's see how to store the values of the boxes from the Qshift on the variable ***profile.teamSchedules.queues***

let's use the boxes for JLOPEZRE for monday tuesday and thursday

![[Pasted image 20250128170953.png]]

So, again checking ***addEngineerQshiftToProfile()***, what we do interest is in ***addIdto***

```js
function addEngineerQshiftToProfile(){

    var queueCheckModal = document.querySelector("#engineer-qshift-modal")
    const checkedBoxesDom = queueCheckModal.querySelectorAll('input[type="checkbox"]:checked')
    var addIdto = []
    for (var inputDom of checkedBoxesDom){
        addIdto.push(inputDom.parentElement.parentElement.id.replace("-select",""))
    }
```

Which seems to contain an array of "Q + day"

![[Pasted image 20250128171710.png]]


Let's gonna delete the other content and keep the function like

```js
function addEngineerQshiftToProfile(){

    var queueCheckModal = document.querySelector("#engineer-qshift-modal")
    const checkedBoxesDom = queueCheckModal.querySelectorAll('input[type="checkbox"]:checked')
    var addIdto = []
    for (var inputDom of checkedBoxesDom){
        addIdto.push(inputDom.parentElement.parentElement.id.replace("-select",""))
    }

⇒ ⇒ ⇒ ⇒ ⇒ ⇒ ⇒ ⇒ CODE WILL BE HERE ⇐ ⇐ ⇐ ⇐ ⇐ ⇐ ⇐ ⇐ 
 
    populateQueueTables()
}
```

Let's try to move this 

```json
 ['Q1-monday', 'Q1-tuesday', 'Q1-thursday', 'Q2-monday', 'Q2-friday']
```

Into ***profile.teamSchedules.queues***


Finally it works, code below

```js
function updateTeamSchedules(engineerName, inputQDays) {
    // Convert inputQDays into the desired grouped format
    const groupedDays = inputQDays.reduce((acc, item) => {
        const [queue, day] = item.split('-');
        const group = acc.find(([key]) => key === queue);
        if (group) {
            group[1].push(day);
        } else {
            acc.push([queue, [day]]);
        }
        return acc;
    }, []);

    // Iterate through profile.teamSchedules.queues to update engineers
    groupedDays.forEach(([queue, days]) => {
        // Find the matching queue in the profile
        const queueObj = profile.teamSchedules.queues.find(q => q.queue === queue);
        if (queueObj) {
            // Iterate through the days in the queue and update engineers
            queueObj.days.forEach(dayObj => {
                if (days.includes(dayObj.day)) {
                    // Add engineerName to the engineers array if it's not already there
                    if (!dayObj.engineers.includes(engineerName)) {
                        dayObj.engineers.push(engineerName);
                    }
                }
            });
        }
    });
}


function addEngineerQshiftToProfile(){

    var queueCheckModal = document.querySelector("#engineer-qshift-modal")
    const checkedBoxesDom = queueCheckModal.querySelectorAll('input[type="checkbox"]:checked')
	const engineerID = queueCheckModal.querySelector("#qshift-modal-title").textContent.match(/for (\w+)/)[1];

    var addIdto = []
    for (var inputDom of checkedBoxesDom){
        addIdto.push(inputDom.parentElement.parentElement.id.replace("-select",""))
    }

	updateTeamSchedules(engineerName, inputQDays);

    populateQueueTables()
}
```



GitHub Copilot: Here are five simpler tasks based on the provided job:

1. **Task 1: Update Modal Button Click Handler**
   - Ensure the modal button click handler correctly calls `addEngineerQshiftToProfile()`.
   - Code Reference:
     ```html
     <div class="modal__footer">
         <button class="btn" onclick="toggleModal('engineer-qshift-modal'), addEngineerQshiftToProfile()">Ok</button>
     </div>
     ```

2. **Task 2: Fix Loop in `addEngineerQshiftToProfile`**
   - Fix the loop in `addEngineerQshiftToProfile` to correctly gather selected checkboxes and store their IDs.
   - Code Reference:
     ```javascript
     function addEngineerQshiftToProfile(){
         var queueCheckModal = document.querySelector("#engineer-qshift-modal")
         const checkedBoxesDom = queueCheckModal.querySelectorAll('input[type="checkbox"]:checked')
         var addIdto = []
         for (var inputDom of checkedBoxesDom){
             addIdto.push(inputDom.parentElement.parentElement.id.replace("-select",""))
         }
     ```

3. **Task 3: Implement `updateTeamSchedules` Function**
   - Implement the `updateTeamSchedules` function to update the `profile.teamSchedules.queues` with the selected engineer and days.
   - Code Reference:
     ```javascript
     function updateTeamSchedules(engineerName, inputQDays) {
         const groupedDays = inputQDays.reduce((acc, item) => {
             const [queue, day] = item.split('-');
             const group = acc.find(([key]) => key === queue);
             if (group) {
                 group[1].push(day);
             } else {
                 acc.push([queue, [day]]);
             }
             return acc;
         }, []);

         groupedDays.forEach(([queue, days]) => {
             const queueObj = profile.teamSchedules.queues.find(q => q.queue === queue);
             if (queueObj) {
                 queueObj.days.forEach(dayObj => {
                     if (days.includes(dayObj.day)) {
                         if (!dayObj.engineers.includes(engineerName)) {
                             dayObj.engineers.push(engineerName);
                         }
                     }
                 });
             }
         });
     }
     ```

4. **Task 4: Integrate `updateTeamSchedules` in `addEngineerQshiftToProfile`**
   - Integrate the `updateTeamSchedules` function within `addEngineerQshiftToProfile` to update the profile with the selected engineer and days.
   - Code Reference:
     ```javascript
     function addEngineerQshiftToProfile(){
         var queueCheckModal = document.querySelector("#engineer-qshift-modal")
         const checkedBoxesDom = queueCheckModal.querySelectorAll('input[type="checkbox"]:checked')
         const engineerID = queueCheckModal.querySelector("#qshift-modal-title").textContent.match(/for (\w+)/)[1];

         var addIdto = []
         for (var inputDom of checkedBoxesDom){
             addIdto.push(inputDom.parentElement.parentElement.id.replace("-select",""))
         }

         updateTeamSchedules(engineerID, addIdto);

         populateQueueTables()
     }
     ```

5. **Task 5: Populate Queue Tables**
   - Ensure the `populateQueueTables` function is called to refresh the UI with the updated queue data.
   - Code Reference:
     ```javascript
     function addEngineerQshiftToProfile(){
         var queueCheckModal = document.querySelector("#engineer-qshift-modal")
         const checkedBoxesDom = queueCheckModal.querySelectorAll('input[type="checkbox"]:checked')
         const engineerID = queueCheckModal.querySelector("#qshift-modal-title").textContent.match(/for (\w+)/)[1];

         var addIdto = []
         for (var inputDom of checkedBoxesDom){
             addIdto.push(inputDom.parentElement.parentElement.id.replace("-select",""))
         }

         updateTeamSchedules(engineerID, addIdto);

         populateQueueTables()
     }
     ```

These tasks break down the implementation into more manageable pieces that can be tracked and completed individually.

aubarnes_cisco: Do the same for this: 

Currently at *table.js* at **updateTeamschedules** at 307 lines. we have 

```js
// Iterate through profile.teamSchedules.queues to update engineers
    groupedDays.forEach(([queue, days]) => {
        // Find the matching queue in the profile
        const queueObj = profile.teamSchedules.queues.find(q => q.queue === queue);
        if (queueObj) {
            // Iterate through the days in the queue and update engineers
            queueObj.days.forEach(dayObj => {
                if (days.includes(dayObj.day)) {
                    // Add engineerName to the engineers array if it's not already there
                    if (!dayObj.engineers.includes(engineerName)) {
                        dayObj.engineers.push(engineerName);
                    }
                }
            });
        }
    });
```

So if we open the qfshit modal

![[Pasted image 20250128192755.png]]


At Monday to Thursday is ticked, however if we unticked one of those and click on save, it does not remove the one ones we unticked.

![[Pasted image 20250128192859.png]]


So, we change for 

```js
// Iterate through the grouped queues and update the profile
     groupedDays.forEach(([queue, days]) => {
        // Find the matching queue in the profile
        const queueObj = profile.teamSchedules.queues.find(q => q.queue === queue);
        if (queueObj) {
            // Remove the engineer from all days of this queue
            queueObj.days.forEach(dayObj => {
                dayObj.engineers = dayObj.engineers.filter(e => e !== engineerName);
            });

            // Add the engineer only to the specified days
            queueObj.days.forEach(dayObj => {
                if (days.includes(dayObj.day)) {
                    if (!dayObj.engineers.includes(engineerName)) {
                        dayObj.engineers.push(engineerName);
                    }
                }
            });
        }
    });
```

Now it works

GitHub Copilot: Here are five simpler tasks based on the provided job:

1. **Task 1: Identify and Group Selected Days**
   - Identify and group the selected days for each queue from the input.
   - Code Reference:
     ```javascript
     const groupedDays = inputQDays.reduce((acc, item) => {
         const [queue, day] = item.split('-');
         const group = acc.find(([key]) => key === queue);
         if (group) {
             group[1].push(day);
         } else {
             acc.push([queue, [day]]);
         }
         return acc;
     }, []);
     ```

2. **Task 2: Find Matching Queue in Profile**
   - Find the matching queue in the `profile.teamSchedules.queues` array.
   - Code Reference:
     ```javascript
     groupedDays.forEach(([queue, days]) => {
         const queueObj = profile.teamSchedules.queues.find(q => q.queue === queue);
     ```

3. **Task 3: Remove Engineer from All Days**
   - Remove the engineer from all days of the matched queue.
   - Code Reference:
     ```javascript
     if (queueObj) {
         queueObj.days.forEach(dayObj => {
             dayObj.engineers = dayObj.engineers.filter(e => e !== engineerName);
         });
     ```

4. **Task 4: Add Engineer to Specified Days**
   - Add the engineer only to the specified days in the matched queue.
   - Code Reference:
     ```javascript
     queueObj.days.forEach(dayObj => {
         if (days.includes(dayObj.day)) {
             if (!dayObj.engineers.includes(engineerName)) {
                 dayObj.engineers.push(engineerName);
             }
         }
     });
     ```

5. **Task 5: Integrate and Test the Updated Logic**
   - Integrate the updated logic into the `updateTeamSchedules` function and test to ensure it works as expected.
   - Code Reference:
     ```javascript
     function updateTeamSchedules(engineerName, inputQDays) {
         const groupedDays = inputQDays.reduce((acc, item) => {
             const [queue, day] = item.split('-');
             const group = acc.find(([key]) => key === queue);
             if (group) {
                 group[1].push(day);
             } else {
                 acc.push([queue, [day]]);
             }
             return acc;
         }, []);

         groupedDays.forEach(([queue, days]) => {
             const queueObj = profile.teamSchedules.queues.find(q => q.queue === queue);
             if (queueObj) {
                 queueObj.days.forEach(dayObj => {
                     dayObj.engineers = dayObj.engineers.filter(e => e !== engineerName);
                 });

                 queueObj.days.forEach(dayObj => {
                     if (days.includes(dayObj.day)) {
                         if (!dayObj.engineers.includes(engineerName)) {
                             dayObj.engineers.push(engineerName);
                         }
                     }
                 });
             }
         });
     }
     ```

These tasks break down the implementation into more manageable pieces that can be tracked and completed individually.